# Portfolio Site - Frontend Архитектор & Ментор

Современный одностраничный сайт-визитка для IT-специалиста, созданный с использованием React, TypeScript, Vite, Ant Design и Tailwind CSS.

## 🚀 Технологический стек

- **React 18** + **TypeScript** - основной фреймворк
- **Vite** - быстрый билд-инструмент
- **Ant Design** - UI-библиотека для сложных компонентов
- **Tailwind CSS** - утилитарный CSS-фреймворк
- **React Router DOM** - роутинг для SPA
- **Zustand** - управление состоянием
- **React Hook Form** + **Yup** - формы и валидация
- **ESLint** + **Prettier** - линтинг и форматирование
- **Husky** - pre-commit hooks

## 📦 Установка

```bash
npm install
```

## 🛠️ Разработка

```bash
npm run dev
```

Сайт будет доступен по адресу `http://localhost:5173`

## 🏗️ Сборка

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 📝 Скрипты

- `npm run dev` - запуск dev-сервера
- `npm run build` - сборка для продакшена
- `npm run preview` - предпросмотр собранного проекта
- `npm run lint` - проверка кода линтером
- `npm run format` - форматирование кода

## 🌐 Деплой на GitHub Pages

1. Установите `gh-pages`:
```bash
npm install --save-dev gh-pages
```

2. Добавьте в `package.json`:
```json
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d dist"
}
```

3. В `vite.config.ts` установите `base`:
```typescript
export default defineConfig({
  base: '/your-repo-name/',
  // ...
})
```

4. Деплой:
```bash
npm run deploy
```

## 📁 Структура проекта

```
src/
├── components/       # React компоненты
│   ├── Hero.tsx
│   ├── ExperienceTimeline.tsx
│   ├── TechStack.tsx
│   ├── Principles.tsx
│   ├── Contact.tsx
│   └── Layout.tsx
├── pages/           # Страницы
│   └── Home.tsx
├── store/           # Zustand store
│   └── useAppStore.ts
├── App.tsx          # Главный компонент
├── main.tsx         # Точка входа
└── index.css        # Глобальные стили
```

## 🎨 Особенности

- **Интерактивная временная шкала** опыта работы с раскрывающимися карточками
- **Облако тегов** технологий с фильтрацией по категориям
- **Модальное окно** с объяснением замыканий (для менторской секции)
- **Адаптивный дизайн** для всех устройств
- **Плавная прокрутка** между секциями
- **Темная тема** с градиентами

## 📝 Примечания

- Данные опыта работы и контакты нужно будет обновить в соответствующих компонентах
- Форма контакта в текущей версии только логирует данные (нужно подключить API)
- Все ссылки на соцсети нужно заменить на реальные

