# Исправление 404 на GitHub Pages

## 🔴 Проблема

GitHub Pages показывает 404, потому что настроен деплой из ветки `main` (исходники), а не из собранного проекта.

## ✅ Решение

### Шаг 1: Обновите настройки GitHub Pages

1. Зайдите в ваш репозиторий: `https://github.com/BillShifr/tatyankin-portfolio`
2. Перейдите в **Settings** → **Pages**
3. В разделе **Source** измените:
   - **Branch:** выберите `gh-pages` (вместо `main`)
   - **Folder:** выберите `/ (root)`
4. Нажмите **Save**

### Шаг 2: Запустите деплой вручную

После изменения настроек нужно запустить деплой:

1. Зайдите во вкладку **Actions** в вашем репозитории
2. Если есть незавершенные workflow - дождитесь их завершения
3. Если нет активных workflow, сделайте новый push:

```bash
# Сделайте небольшое изменение (например, добавьте пробел в README)
git add .
git commit -m "Trigger deployment"
git push origin main
```

### Шаг 3: Дождитесь деплоя

1. Зайдите во вкладку **Actions**
2. Дождитесь завершения workflow "Deploy to GitHub Pages"
3. Должна появиться зеленая галочка ✅

### Шаг 4: Проверьте сайт

После успешного деплоя (обычно 1-2 минуты) сайт должен быть доступен:

**https://billshifr.github.io/tatyankin-portfolio/**

## 🔍 Проверка

Если все еще 404:

1. **Проверьте ветку `gh-pages`:**
   - В репозитории переключитесь на ветку `gh-pages` (вверху слева)
   - Убедитесь, что там есть файлы из папки `dist` (index.html, assets и т.д.)

2. **Проверьте base path:**
   - Убедитесь, что в `vite.config.ts` стоит `base: '/tatyankin-portfolio/'`
   - Это должно соответствовать имени вашего репозитория

3. **Проверьте логи деплоя:**
   - Зайдите в **Actions** → выберите последний workflow
   - Проверьте, нет ли ошибок в логах

## 📝 Что я изменил

Обновил `.github/workflows/deploy.yml` для использования нового GitHub Actions API для деплоя на Pages. Теперь он:
- Использует официальный GitHub Actions для деплоя
- Правильно настраивает permissions
- Деплоит в ветку `gh-pages` автоматически

## ⚠️ Важно

После изменения настроек GitHub Pages на ветку `gh-pages`, GitHub может показать предупреждение о том, что ветка не существует. Это нормально - ветка будет создана автоматически после первого успешного деплоя через Actions.
