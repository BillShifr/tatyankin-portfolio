# Деплой на GitHub Pages

## 📋 Шаг 1: Настройка репозитория

1. **Убедитесь, что код отправлен в GitHub:**

   ```bash
   git add .
   git commit -m "Configure for GitHub Pages"
   git push -u origin main
   ```

2. **Включите GitHub Pages:**
   - Зайдите в ваш репозиторий на GitHub: `https://github.com/BillShifr/tatyankin-portfolio`
   - Перейдите в **Settings** (вверху справа)
   - В левом меню найдите **Pages**
   - В разделе **Source**:
     - Branch: выберите `gh-pages`
     - Folder: выберите `/ (root)`
   - Нажмите **Save**

## 🚀 Шаг 2: Автоматический деплой

GitHub Actions автоматически задеплоит ваш сайт при каждом push в ветку `main`.

### Как это работает:

1. При каждом `git push` в ветку `main`
2. GitHub Actions запускает workflow (файл `.github/workflows/deploy.yml`)
3. Собирает проект (`npm run build`)
4. Деплоит в ветку `gh-pages`
5. Сайт становится доступным

## 🌐 Шаг 3: Доступ к сайту

После первого деплоя (обычно 1-2 минуты) ваш сайт будет доступен по адресу:

**https://billshifr.github.io/tatyankin-portfolio/**

> ⚠️ **Важно:** URL формируется как `username.github.io/repository-name/`

## 📝 Проверка статуса деплоя

1. Зайдите в ваш репозиторий на GitHub
2. Перейдите во вкладку **Actions** (вверху)
3. Там вы увидите статус деплоя:
   - 🟡 Желтый кружок = деплой в процессе
   - ✅ Зеленая галочка = деплой успешен
   - ❌ Красный крестик = ошибка (нажмите для просмотра логов)

## 🔄 Обновление сайта

После каждого изменения просто делайте:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

GitHub Actions автоматически задеплоит обновления (обычно 1-2 минуты).

## ⚙️ Текущая конфигурация

- **Base path:** `/tatyankin-portfolio/` (в `vite.config.ts`)
- **Build command:** `npm run build`
- **Output directory:** `dist`
- **Deploy branch:** `gh-pages`

## 🔧 Если что-то не работает

### Проблема: Сайт показывает 404

**Решение:** Проверьте, что `base` в `vite.config.ts` соответствует имени репозитория:
- Репозиторий `tatyankin-portfolio` → `base: '/tatyankin-portfolio/'`
- Репозиторий `username.github.io` → `base: '/'`

### Проблема: Деплой не запускается

**Решение:**
1. Проверьте вкладку **Actions** - есть ли ошибки
2. Убедитесь, что файл `.github/workflows/deploy.yml` существует
3. Проверьте, что вы пушите в ветку `main`

### Проблема: Стили не загружаются

**Решение:** Убедитесь, что `base` в `vite.config.ts` правильный и пересоберите проект.

## 📦 Локальная проверка перед деплоем

Перед первым деплоем проверьте локально:

```bash
# Соберите проект
npm run build

# Запустите предпросмотр
npm run preview
```

Откройте `http://localhost:4173/tatyankin-portfolio/` и убедитесь, что все работает.

## ✅ Готово!

После настройки GitHub Pages ваш сайт будет автоматически обновляться при каждом push в `main` ветку.
