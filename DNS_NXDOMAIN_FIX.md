# Исправление ошибки NXDOMAIN

## 🔴 Ошибка: "server can't find tatyankin-portfolio.ru: NXDOMAIN"

Эта ошибка означает, что DNS-сервер не может найти ваш домен. Возможные причины:

## ✅ Решения

### 1. Проверьте через публичные DNS серверы

Ваш локальный DNS (192.168.0.1) может еще не обновиться. Проверьте через публичные DNS:

```bash
# Через Google DNS
nslookup tatyankin-portfolio.ru 8.8.8.8

# Через Cloudflare DNS
nslookup tatyankin-portfolio.ru 1.1.1.1
```

### 2. Проверьте онлайн

Проверьте через онлайн-сервисы:
- https://dnschecker.org/#A/tatyankin-portfolio.ru
- https://www.whatsmydns.net/#A/tatyankin-portfolio.ru

### 3. Проверьте настройки в Рег.ру

#### 3.1. Убедитесь, что домен активен

1. Зайдите в личный кабинет Рег.ру
2. Проверьте статус домена `tatyankin-portfolio.ru`
3. Убедитесь, что домен не заблокирован и не на удержании

#### 3.2. Проверьте DNS-серверы

1. В настройках домена проверьте, что указаны:
   - `ns1.hosting.reg.ru`
   - `ns2.hosting.reg.ru`

2. Если указаны другие DNS-серверы, измените на серверы Рег.ру

#### 3.3. Проверьте DNS записи

1. Зайдите в **DNS-серверы и зона** → **Управление DNS-записями**
2. Убедитесь, что добавлены A-записи:

   **Запись 1:**
   - Тип: **A**
   - Имя: **@** (или пусто, или `tatyankin-portfolio.ru`)
   - Значение: **185.199.108.153**
   - TTL: **3600**

   **Запись 2:**
   - Тип: **A**
   - Имя: **@**
   - Значение: **185.199.109.153**
   - TTL: **3600**

   **Запись 3:**
   - Тип: **A**
   - Имя: **@**
   - Значение: **185.199.110.153**
   - TTL: **3600**

   **Запись 4:**
   - Тип: **A**
   - Имя: **@**
   - Значение: **185.199.111.153**
   - TTL: **3600**

3. **Сохраните изменения**

### 4. Подождите распространения DNS

После добавления/изменения DNS записей:
- **Минимум:** 10-30 минут
- **Обычно:** 1-2 часа
- **Максимум:** до 48 часов

### 5. Очистите кеш DNS на вашем компьютере

**macOS:**
```bash
sudo dscacheutil -flushcache
sudo killall -HUP mDNSResponder
```

**Windows:**
```cmd
ipconfig /flushdns
```

**Linux:**
```bash
sudo systemd-resolve --flush-caches
```

### 6. Проверьте через dig (если установлен)

```bash
dig @8.8.8.8 tatyankin-portfolio.ru
dig @1.1.1.1 tatyankin-portfolio.ru
```

## 🔍 Дополнительная диагностика

### Проверка делегирования домена

```bash
# Проверка NS записей
dig NS tatyankin-portfolio.ru

# Должны показаться:
# ns1.hosting.reg.ru
# ns2.hosting.reg.ru
```

### Проверка A записей напрямую

```bash
# Через DNS серверы Рег.ру
dig @ns1.hosting.reg.ru tatyankin-portfolio.ru
dig @ns2.hosting.reg.ru tatyankin-portfolio.ru
```

## ⚠️ Возможные проблемы

1. **Домен не зарегистрирован:** Убедитесь, что домен `tatyankin-portfolio.ru` действительно зарегистрирован в Рег.ру
2. **DNS-серверы не настроены:** Домен должен использовать DNS-серверы Рег.ру
3. **DNS записи не сохранены:** Убедитесь, что нажали "Сохранить" после добавления записей
4. **TTL слишком большой:** Используйте TTL 3600 или меньше

## 📞 Если ничего не помогает

1. **Обратитесь в поддержку Рег.ру:**
   - Они могут проверить настройки DNS на их стороне
   - Возможно, есть какие-то особенности для доменов .ru

2. **Проверьте, что домен активен:**
   - В личном кабинете Рег.ру проверьте статус домена
   - Убедитесь, что домен не на удержании и не заблокирован

3. **Попробуйте через другой DNS провайдер:**
   - Временно измените DNS-серверы на Cloudflare или другие
   - Настройте A-записи там
   - После проверки вернитесь к Рег.ру
