# Настройка кастомного домена для GitHub Pages

## 🎯 Цель

Изменить URL с `https://billshifr.github.io/tatyankin-portfolio/` на `https://tatyankin-portfolio` (или `https://www.tatyankin-portfolio`)

## 📋 Шаг 1: Купить домен (если еще нет)

Если у вас еще нет домена `tatyankin-portfolio`, нужно его купить:
- **Namecheap** (рекомендую)
- **GoDaddy**
- **Reg.ru** (для .ru доменов)
- **Другие регистраторы**

## ⚙️ Шаг 2: Настроить DNS записи

После покупки домена нужно настроить DNS записи:

### Вариант A: Без www (tatyankin-portfolio)

Добавьте следующие DNS записи:

**Тип A записи:**
```
Имя: @ (или пусто)
Значение: 185.199.108.153
TTL: 3600 (или Auto)
```

**Дополнительные A записи (для надежности):**
```
@ → 185.199.109.153
@ → 185.199.110.153
@ → 185.199.111.153
```

**CNAME запись (опционально, для www):**
```
Имя: www
Значение: billshifr.github.io
TTL: 3600
```

### Вариант B: С www (www.tatyankin-portfolio)

**CNAME запись:**
```
Имя: www (или @)
Значение: billshifr.github.io
TTL: 3600
```

## 🔧 Шаг 3: Обновить конфигурацию проекта

После настройки DNS нужно обновить конфигурацию проекта, чтобы `base` был `/` вместо `/tatyankin-portfolio/`:

### 3.1. Обновить `vite.config.ts`:

```typescript
export default defineConfig(({ command }) => ({
  plugins: [...],
  // Для кастомного домена base должен быть '/'
  base: '/',
  // ...
}))
```

### 3.2. Обновить `src/main.tsx`:

```typescript
// Для кастомного домена basename должен быть '/'
const basename = '/'
```

## 🌐 Шаг 4: Настроить кастомный домен в GitHub

1. Зайдите в ваш репозиторий: `https://github.com/BillShifr/tatyankin-portfolio`
2. Перейдите в **Settings** → **Pages**
3. В разделе **Custom domain**:
   - Введите ваш домен: `tatyankin-portfolio` (или `www.tatyankin-portfolio`)
   - Нажмите **Save**
4. GitHub автоматически создаст файл `CNAME` в репозитории

## ⏳ Шаг 5: Дождаться распространения DNS

DNS изменения могут занять от нескольких минут до 48 часов. Обычно это занимает 1-2 часа.

Проверить можно командой:
```bash
nslookup tatyankin-portfolio
```

Или через онлайн-сервисы:
- https://dnschecker.org
- https://www.whatsmydns.net

## ✅ Шаг 6: Проверить SSL сертификат

GitHub автоматически выдаст SSL сертификат для вашего домена (обычно через несколько минут после настройки).

Проверьте, что сайт открывается по HTTPS:
- `https://tatyankin-portfolio` ✅
- `http://tatyankin-portfolio` (должен редиректить на HTTPS)

## 🔄 Шаг 7: Обновить код и задеплоить

После настройки DNS и домена в GitHub:

1. Обновите конфигурацию (см. Шаг 3)
2. Закоммитьте и запушьте изменения:
   ```bash
   git add .
   git commit -m "Configure for custom domain"
   git push origin main
   ```

## 📝 Важные замечания

1. **DNS изменения:** Могут занять до 48 часов, обычно 1-2 часа
2. **SSL сертификат:** GitHub выдает автоматически, обычно через 10-30 минут
3. **Base path:** При использовании кастомного домена `base` должен быть `/`
4. **CNAME файл:** GitHub создаст его автоматически, не удаляйте его
5. **Редирект:** GitHub автоматически редиректит с HTTP на HTTPS

## 🆘 Если что-то не работает

1. **Проверьте DNS:**
   ```bash
   nslookup tatyankin-portfolio
   dig tatyankin-portfolio
   ```

2. **Проверьте SSL:**
   - Убедитесь, что в настройках GitHub Pages включен "Enforce HTTPS"

3. **Проверьте файл CNAME:**
   - В репозитории должен быть файл `CNAME` в корне или в папке `docs`
   - Содержимое: `tatyankin-portfolio` (без http:// и https://)

4. **Очистите кеш браузера:**
   - Попробуйте открыть в режиме инкогнито
   - Или очистите кеш DNS: `sudo dscacheutil -flushcache` (macOS)
