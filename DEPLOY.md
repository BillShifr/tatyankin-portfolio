# Инструкция по деплою портфолио

## 🚀 Вариант 1: Vercel (РЕКОМЕНДУЕТСЯ - самый простой)

### Шаги:

1. **Убедитесь, что проект в Git:**
   ```bash
   git status
   git add .
   git commit -m "Ready for deployment"
   git push origin main
   ```

2. **Зайдите на [vercel.com](https://vercel.com)**

3. **Войдите через GitHub**

4. **Нажмите "Add New Project"**

5. **Выберите ваш репозиторий `portfolio-site`**

6. **Настройки проекта:**
   - Framework Preset: **Vite**
   - Root Directory: `./` (оставьте по умолчанию)
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`

7. **Нажмите "Deploy"**

8. **Готово!** Сайт будет доступен по адресу `portfolio-site-xxx.vercel.app`

### Кастомный домен (опционально):
- В настройках проекта → Domains
- Добавьте свой домен

---

## 🌐 Вариант 2: Netlify

### Шаги:

1. **Убедитесь, что проект в Git** (см. шаг 1 из Vercel)

2. **Зайдите на [netlify.com](https://netlify.com)**

3. **Войдите через GitHub**

4. **Нажмите "Add new site" → "Import an existing project"**

5. **Выберите репозиторий**

6. **Настройки:**
   - Build command: `npm run build`
   - Publish directory: `dist`

7. **Нажмите "Deploy site"**

8. **Готово!** Сайт будет доступен по адресу `portfolio-site-xxx.netlify.app`

---

## 📦 Вариант 3: GitHub Pages

### Шаги:

1. **Обновите `vite.config.ts`:**
   - Если репозиторий называется `portfolio-site`, base должен быть `/portfolio-site/`
   - Если репозиторий называется `username.github.io`, base должен быть `/`

2. **Включите GitHub Pages:**
   - Зайдите в Settings вашего репозитория на GitHub
   - Pages → Source: выберите `gh-pages` branch
   - Сохраните

3. **Закоммитьте и запушьте изменения:**
   ```bash
   git add .
   git commit -m "Configure for GitHub Pages"
   git push origin main
   ```

4. **GitHub Actions автоматически задеплоит проект**

5. **Сайт будет доступен по адресу:**
   - `https://username.github.io/portfolio-site/` (если репозиторий называется portfolio-site)
   - `https://username.github.io/` (если репозиторий называется username.github.io)

---

## 🔧 Локальная проверка перед деплоем

Перед деплоем проверьте, что все работает локально:

```bash
# Установите зависимости (если еще не установлены)
npm install

# Соберите проект
npm run build

# Проверьте сборку локально
npm run preview
```

Откройте `http://localhost:4173` и убедитесь, что все работает корректно.

---

## 📝 Полезные команды

```bash
# Разработка
npm run dev

# Сборка для продакшена
npm run build

# Предпросмотр продакшен-сборки
npm run preview

# Линтинг
npm run lint

# Форматирование
npm run format
```

---

## ⚠️ Важные замечания

1. **Для GitHub Pages:** Убедитесь, что `base` в `vite.config.ts` соответствует имени вашего репозитория
2. **Для Vercel/Netlify:** `base` должен быть `/`
3. **Все изменения автоматически деплоятся** при пуше в main ветку (если настроен CI/CD)
4. **Проверьте, что все зависимости указаны в `package.json`**

---

## 🎯 Рекомендация

**Используйте Vercel** - это самый простой и быстрый способ для React/Vite приложений:
- Автоматический деплой при каждом пуше
- Бесплатный SSL сертификат
- Быстрая CDN
- Простая настройка кастомных доменов
- Отличная производительность
