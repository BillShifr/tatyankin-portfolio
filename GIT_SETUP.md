# Настройка Git и подключение к GitHub

## Проблема: Permission denied (publickey)

Эта ошибка означает, что у вас не настроен SSH ключ для GitHub. Есть два решения:

---

## ✅ Решение 1: Использовать HTTPS (ПРОЩЕ)

### Шаги:

1. **Измените URL удаленного репозитория на HTTPS:**

   ```bash
   git remote set-url origin https://github.com/BillShifr/tatyankin-portfolio.git
   ```

2. **Проверьте, что изменилось:**

   ```bash
   git remote -v
   ```

   Должно показать:
   ```
   origin  https://github.com/BillShifr/tatyankin-portfolio.git (fetch)
   origin  https://github.com/BillShifr/tatyankin-portfolio.git (push)
   ```

3. **Добавьте все файлы и сделайте коммит:**

   ```bash
   git add .
   git commit -m "Initial commit: portfolio site"
   ```

4. **Отправьте на GitHub:**

   ```bash
   git push -u origin main
   ```

5. **При первом push GitHub попросит авторизацию:**
   - Введите ваш GitHub username
   - Введите Personal Access Token (см. ниже как создать)

---

## 🔑 Создание Personal Access Token для HTTPS

Если GitHub попросит пароль, используйте Personal Access Token:

1. **Зайдите на GitHub.com** → Settings → Developer settings → Personal access tokens → Tokens (classic)

2. **Нажмите "Generate new token (classic)"**

3. **Настройки:**
   - Note: `Portfolio site deployment`
   - Expiration: выберите срок (например, 90 дней)
   - Scopes: отметьте `repo` (полный доступ к репозиториям)

4. **Нажмите "Generate token"**

5. **Скопируйте токен** (он показывается только один раз!)

6. **При push используйте:**
   - Username: ваш GitHub username
   - Password: вставьте токен (не ваш пароль!)

---

## 🔐 Решение 2: Настроить SSH ключ (для будущего)

Если хотите использовать SSH (удобнее для постоянной работы):

### Шаги:

1. **Создайте SSH ключ:**

   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

   - Нажмите Enter для сохранения в стандартное место
   - Можете установить пароль или оставить пустым

2. **Запустите ssh-agent:**

   ```bash
   eval "$(ssh-agent -s)"
   ```

3. **Добавьте ключ в ssh-agent:**

   ```bash
   ssh-add ~/.ssh/id_ed25519
   ```

4. **Скопируйте публичный ключ:**

   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

   Скопируйте весь вывод (начинается с `ssh-ed25519...`)

5. **Добавьте ключ в GitHub:**
   - Зайдите на GitHub.com → Settings → SSH and GPG keys
   - Нажмите "New SSH key"
   - Title: `MacBook Air`
   - Key: вставьте скопированный ключ
   - Нажмите "Add SSH key"

6. **Проверьте подключение:**

   ```bash
   ssh -T git@github.com
   ```

   Должно показать: `Hi BillShifr! You've successfully authenticated...`

7. **Теперь можно использовать SSH URL:**

   ```bash
   git remote set-url origin git@github.com:BillShifr/tatyankin-portfolio.git
   git push -u origin main
   ```

---

## 📝 Полная последовательность команд (HTTPS вариант)

```bash
# Переключитесь на HTTPS
git remote set-url origin https://github.com/BillShifr/tatyankin-portfolio.git

# Проверьте
git remote -v

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Initial commit: portfolio site"

# Отправьте на GitHub
git push -u origin main
```

---

## ⚠️ Если возникнут проблемы

1. **Проверьте, что репозиторий существует на GitHub**
2. **Убедитесь, что у вас есть права на запись в репозиторий**
3. **Для HTTPS: используйте Personal Access Token вместо пароля**
4. **Для SSH: убедитесь, что ключ добавлен в GitHub**

---

## 🎯 Рекомендация

**Используйте HTTPS** - это проще для начала. SSH можно настроить позже, если понадобится.
